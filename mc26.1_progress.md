# Прогресс перехода на MC 26.1

## Подготовка и файлы сборки

- [x] Создана ветка `mc-26.1-migration`
- [x] Найдены актуальные версии зависимостей для 26.1:
  - Loom `1.16.1`
  - Fabric Loader `0.18.6`
  - Fabric API `0.145.1+26.1`
- [x] Создан `build-nextgen.gradle` (plugin ID `net.fabricmc.fabric-loom`, Java 25, без `mappings`, `implementation` вместо `modImplementation`)
- [x] Обновлён `gradle.properties` — добавлены версии для nextgen профиля
- [x] Обновлён `fabric.mod.json` — шаблонные переменные `${java_min_version}` и `${minecraft_min_version}`
- [x] Обновлён `build.gradle` — processResources расширен новыми переменными (чтобы старые профили не сломались)
- [x] Обновлён `settings.gradle` — добавлен `mavenCentral()` в pluginManagement
- [x] Создан `src/compat/nextgen/.../VersionCompat.java` (с `KeyMappingHelper` вместо `KeyBindingHelper`)

## Компиляция и миграция

- [ ] **Первый `compileJava`** — заблокировано: Gradle берёт Loom `1.14.10` из кеша вместо `1.16.1`; нужно решить конфликт версий плагина
- [ ] Запустить `migrateMappings` — переименует Yarn → Mojang имена в `src/main/java/` и `src/compat/nextgen/`
- [ ] Вручную исправить Fabric API рефакторинги (migrateMappings их не трогает):
  - [ ] `KeyBindingHelper` → `KeyMappingHelper` (пакет + класс + метод) — уже в nextgen/VersionCompat, проверить после migrateMappings
  - [ ] `ClientTickEvents.START_WORLD_TICK` → `START_LEVEL_TICK` в `ResourceTrackerClient.java`
  - [ ] `ClientTickEvents.END_WORLD_TICK` → `END_LEVEL_TICK` в `ResourceTrackerClient.java`
- [ ] Устранить оставшиеся ошибки компиляции после migrateMappings (Registries, DataComponentTypes, SoundEvents и др.)
- [ ] Убедиться что старые профили не сломались: `./gradlew compileJava -Pprofile=modern ...`

## CI и финал

- [ ] Обновить `.github/workflows/build.yml` — добавить nextgen entry в матрицу, per-entry `java_version`
- [ ] In-game тест через `./gradlew runClient -b build-nextgen.gradle ...`
- [ ] Пройти in-game чеклист (HUD, счётчики, Shulker Box, Bundle, конфиг, ModMenu)

## Заблокировано / ожидает

- [x] ModMenu для 26.1 — `modmenu_version_nextgen=18.0.0-alpha.8`
- [ ] Обновить Gradle wrapper до последней версии (после решения проблемы с Loom)
